#!/bin/sh

echo "------------------------"
echo "How to connect to UPC VPN?"
echo "This VPN works via forticlient (we recommend to use gnome insteda of I3 as a Windows Manager (WM), for connecting to forticlient."
echo "Forticlient Binary: /opt/forticlient/gui/FortiClient-linux-x64/FortiClient"
echo "------------------------"
echo "How to connec to Vedruna25 VPN?"
echo "This VPN works via wireguard, and you can easily connect via terminal."
echo "These are the commands:"
echo "- For checking the status:"
echo "	sudo systemctl status wg-quick@wg0"
echo ""
echo "- For checking the status you can also check the interface wg address"
echo "	sudo ip a show wg0"
echo "- For connecting (if the status shows as deactivated):"
echo "	sudo systemctl start wg-quick@wg0"
echo "- For stopping the VPN :"
echo "	sudo systemctl stop wg-quick@wg0"
echo "- For enabling the VPN :"
echo "	sudo systemctl enable wg-quick@wg0"
echo "More info can be found here: https://www.cyberciti.biz/faq/ubuntu-20-04-set-up-wireguard-vpn-server/"
echo "------------------------"
